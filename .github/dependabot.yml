version: 2

updates:
- package-ecosystem: mix
  directory: '/'
  schedule:
    interval: monthly
  groups:
    dev-dependencies:
      dependency-type: development
      update-types: ['minor', 'patch']
    prod-dependencies:
      dependency-type: production
      update-types: ['minor', 'patch']

- package-ecosystem: github-actions
  directory: '/'
  schedule:
    interval: monthly
  groups:
    setup-actions:
      patterns: ['erlef/setup-beam*', 'actions/cache*']
    other-actions:
      patterns: ['*']
      exclude-patterns: ['erlef/setup-beam*', 'actions/cache*']
